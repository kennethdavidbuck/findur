---
updated: 2026-09-20T10:05
---

- (outcome) The room aligned Epic 0 around secure SnapTrade entry, with kb insisting it must prove the returned token through at least one real purpose-limited provider read rather than stopping at OAuth callback.
- (outcome by bmad-agent-architect) kb flagged SnapTrade request-frequency and rate-limit risk; Epic 0 live evidence must be explicitly initiated and tightly budgeted, while CI stays entirely on WireMock.
- (outcome by bmad-agent-dev) The room separated deployment smoke from provider verification: extend the existing public-shell/health/ready smoke with an expected build tag for both frontend and API, while readiness never calls SnapTrade.
- (moment by bmad-agent-ux-designer) kb proposed an unlinked frontend diagnostic page that fetches backend readiness; the room favored it as a simple same-origin integration proof, provided it exposes only non-sensitive build/health state and is never treated as access control.
- (outcome by bmad-agent-dev) kb chose the full Git commit SHA as canonical build identity; frontend build metadata, API health/readiness, the unlisted status page, and post-deploy smoke must all agree on it.
- (outcome by bmad-agent-architect) kb chose CI-controlled Render deploy hooks for the exact main-branch SHA, with auto-deploy disabled, frontend/API SHA convergence, bounded post-deploy smoke, concurrency cancellation, and no SnapTrade calls in deployment probes.
- (outcome by bmad-agent-pm) kb defined SnapTrade disconnect as leaving Findur: revoke every session, erase provider identity, profile, preferences, financial/derived data and interaction state, and retain at most an unlinkable deleted tombstone to fence delayed work; reauthorization creates a new user.
- (outcome by bmad-agent-pm) kb simplified deletion to destruction-by-default for the MVP: because no real-to-real history exists, SnapTrade disconnect should delete the entire Findur user and every owned record, cache, session, source and derived artifact; retain nothing absent an absolute demonstrated need.
