# Editorial Prose Review — Findur PRD Addendum

This document exists to help product, UX, and architecture collaborators apply source-backed constraints and handoff requirements without displacing the parent PRD as Findur's governing decision record. The audience is human project collaborators. This prose pass follows the prior **Strategic/Context (Pyramid)** structure review.

Style and voice: formal, directive, safety-conscious, and technically precise. Preserve the defined product terminology and capitalization, the meaningful distinctions among *must*, *should*, and *may*, the compact handoff voice, and the concrete examples and checklists. The edits below follow the Microsoft Writing Style Guide by using direct constructions, explicit actors, parallel lists, and the smallest change needed for clarity.

Word metrics: **3,464 words total**. No length target was supplied.

| Pass | Original Text | Revised Text | Changes |
| --- | --- | --- | --- |
| prose | §1.2, surviving “Current boundary” passage — “For this milestone, the only live data belongs to the authenticated owner and may appear exactly in the private Portfolio Showcase and owner's card preview.” | “For this milestone, the only live data belongs to the authenticated owner and may be shown in exact form only in the private Portfolio Showcase and the owner's card preview.” | Clarifies what “exactly” modifies and makes the display boundary explicit. |
| prose | §1.2 — “without increasing or exposing more of their own disclosure” | “without increasing their own Disclosure Level or exposing more of their own data” | Restores parallel verbs and names the two distinct protected changes. |
| prose | §1.2 — “A selection changes only a visibly unsaved preview until Save succeeds; Cancel restores the saved level, and dirty route exit requires a discard-or-stay decision.” | “Until Save succeeds, a selection changes only the preview, which remains visibly marked as unsaved. Cancel restores the saved level, and attempting to leave the route with unsaved changes requires a discard-or-stay decision.” | Removes the ambiguous attachment of “until Save succeeds” and replaces internal shorthand with a direct user action. |
| prose | §1.2 — “Privacy-decreasing saves fail closed: broader active, prefetched, cached, offline, and history-restored representations are suppressed immediately and remain suppressed while purge or persistence retries.” | “Privacy-decreasing saves fail closed: broader representations—whether active, prefetched, cached, offline, or restored from history—are suppressed immediately and remain suppressed while the system retries any failed purge or persistence operation.” | Supplies the missing actor and verb, and makes the modifiers parallel. |
| prose | §1.3 — “potentially market context, only if it remains understandable, timely, and non-advisory.” | “potential use of market context, but only when it remains understandable, timely, and non-advisory.” | Fixes the nonparallel list item while preserving its conditional status. |
| prose | §2.1 — “UX may refine labels, split fields, and validation, but product must confirm the field set before profile story acceptance” | “UX may refine labels and validation and may split fields, but product must confirm the field set before accepting the profile story” | Restores grammatical parallelism and turns the compressed acceptance noun phrase into a direct action. |
| prose | §2.2 — “Back restores the exact safe Swipe Deck position; an unavailable or gated deep link returns to a named recovery destination without exposing protected content.” | “Back restores the exact safe position in the Swipe Deck. Opening an unavailable or gated deep link takes the user to a named recovery destination without exposing protected content.” | Gives the deep-link clause a human actor and avoids making the link itself perform navigation. |
| prose | §2.7, surviving §5 “Post-match communication” passage — “At minimum, the feature bundle must include unmatch, block, report, financial-solicitation rules, disclosure suppression after safety actions, moderation routing, and an accountable response model.” | “At minimum, the feature bundle must include controls to unmatch, block, and report; rules for financial solicitation; disclosure suppression after safety actions; moderation routing; and an accountable response model.” | Makes the mixed verb-and-noun list parallel without changing the required bundle. |
| prose | §2.8, surviving locale/theme subsection — “English and French plus System, Light, and Dark themes are first-cut parity requirements.” | “English and French language support and System, Light, and Dark theme support are first-cut parity requirements.” | States the two forms of support explicitly and removes the ambiguous use of “plus.” |
| prose | §3.1, surviving OAuth-model subsection — “The technical research favors a Commercial-account-owned SnapTrade OAuth application serving Personal SnapTrade users through the authorization code flow with PKCE, with confidential-client operations on the server.” | “The technical research favors a SnapTrade OAuth application owned by a Commercial account and serving Personal SnapTrade users. This model uses the authorization code flow with PKCE and keeps confidential-client operations on the server.” | Unpacks a dense modifier chain and clarifies which model owns each technical characteristic. |
| prose | §3.1, surviving consent subsection — “OAuth continuation authorizes named provider access and only the minimum metadata needed to list eligible accounts.” | “OAuth continuation authorizes access to the named provider and retrieval of only the minimum metadata needed to list eligible accounts.” | Supplies the action that applies to metadata and makes the two authorized acts parallel. |
| prose | §3.1, surviving entry-context subsection — “Any future synthetic-only guest context must be separately sized and remain unable to introduce public signup, durable guest accounts, or any route to owner tokens, payloads, caches, or state.” | “Any future synthetic-only guest context must be sized separately and must not introduce public signup, durable guest accounts, or any route to owner tokens, payloads, caches, or state.” | Replaces an indirect capability construction with a direct prohibition. |
| prose | §3.2 — “Failed purge or persistence retries without restoring broader state. If adding an account or broadening permission fails during retrieval, persistence, or recalculation, the prior narrower committed set remains authoritative until the broader state succeeds and is confirmed; any later broadening requires scoped confirmation.” | “If a purge or persistence operation fails, the system retries it without restoring broader state. If adding an account or broadening permission fails during retrieval, persistence, or recalculation, the prior narrower committed set remains authoritative until the broader update succeeds and is confirmed. Any later broadening requires scoped confirmation.” | Repairs a sentence fragment, identifies the retrying actor, replaces the illogical “state succeeds,” and separates the final rule. |
| prose | §3.3 — “webhooks may signal synchronization lifecycle rather than provide a holdings-change diff.” | “webhooks may signal synchronization lifecycle events rather than provide holdings-change diffs.” | Adds the missing object and makes the comparison grammatical. |
| prose | §3.6 — “In-product event state is authoritative; permission denial, unsupported platforms, delayed delivery, duplicate delivery, or delivery failure cannot lose the event. Events persist until opened, dismissed, or invalidated; acknowledgement clears their unread and badge contribution without deleting the underlying Mutual Match or recovery state.” | “In-product event state is authoritative; denied permission, unsupported platforms, and delayed, duplicate, or failed delivery must not cause the event to be lost. Events persist until opened, dismissed, or invalidated. Acknowledging an event clears its unread state and badge contribution without deleting the underlying Mutual Match or recovery state.” | Replaces an illogical actor, resolves the singular/plural mismatch, and uses the Microsoft spelling “acknowledging.” |
| prose | §3.6 — “Permission requests are contextual and user initiated, categories are separately configurable, and opening a notification passes through authentication and prerequisite gates before restoring a safe destination.” | “Permission requests are contextual and user-initiated. Categories are separately configurable, and opening a notification passes through authentication and prerequisite gates before restoring a safe destination.” | Breaks up three independent requirements and hyphenates the compound modifier. |
| prose | §3.6 — “Architecture should implement delivery, deduplication, and platform fallback without changing the product-owned persistence, dismissal, invalidation, acknowledgement, or badge-clearing contract and without turning notifications into a separate inbox.” | “Architecture should implement delivery, deduplication, and platform fallback without changing the product-defined contract for persistence, dismissal, invalidation, acknowledgment, or badge clearing and without creating a separate notification inbox.” | Clarifies what the product owns, fixes the modifier chain, and applies the Microsoft spelling “acknowledgment.” |
| prose | §4.1 — “The public product therefore requires safety design before Discovery, rather than only after messaging is added.”<br><br>§4.4 — “Before a public product, obtain written confirmation concerning portfolio-derived compatibility, ranking, disclosure, redistribution, and market-data terms.” | §4.1 — “The public product therefore requires safety design before enabling Discovery, not only after adding messaging.”<br><br>§4.4 — “Before launching a public product, obtain written confirmation concerning portfolio-derived compatibility, ranking, disclosure, redistribution, and market-data terms.” | Replaces compressed “before + noun” constructions with the actions that define each boundary. |
| prose | §5 — “monetization and whether any paid model can avoid selling access to people or their financial data; **and**” followed by three more bullets, with no conjunction before the final bullet. | Remove **“and”** from the monetization bullet and add **“and”** before “SnapTrade's hosted MCP server” in the final bullet. | Places the conjunction only before the last item in the list. |

## Summary

**19 prose recommendations** are grouped into the rows above. They correct ambiguity, fragments, nonparallel phrasing, unclear actors, and one list-conjunction error; none changes product meaning. The substitutions produce no meaningful net reduction against the 3,464-word source and introduce no comprehension trade-off. No length target was supplied.
