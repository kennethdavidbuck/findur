---
scope: Whole Findur product architecture
purpose: Build-substrate architecture spine
altitude: feature
updated: 2026-09-19T20:30
---

- (event) Started an interactive create run for the whole Findur product; no prior architecture spine exists.
- (direction) Use the Coaching path: expose load-bearing alternatives and decide them with the user before distilling the spine.
- (assumption) The architecture spine is the only deliverable for now; offer an explanatory rendering later only if the user identifies a separate audience or decision need.
- (constraint) Treat the finalized PRD, UX package, and SnapTrade research as authoritative inputs; inherit settled product and interaction decisions without re-deciding them.
- (decision) Produce the architecture spine first; derive optional C4 diagrams, solution design, or reviewer-facing walkthrough only afterward so they explain rather than create architecture.
- (direction) User's initial stack direction: a likely vanilla Go backend; React/Vite SPA using an accessibility-first component library; frontend assets served by the Go service; JSON:API semantics for the HTTP API; contract-first development with OpenAPI; consider JSON Schema for consistent frontend/backend data validation. These remain candidates pending trade-off review and current-source verification.
- (version) Verified 2026-09-19: current stable Go is 1.27.1; candidate backend baseline is Go 1.27.x.
- (version) Verified 2026-09-19: current stable React is 19.3 and current supported Vite line is 8.3; candidate SPA baseline is React 19.3 with Vite 8.3.
- (version) Verified 2026-09-19: JSON:API latest stable specification is 1.1.
- (version) Verified 2026-09-19: OpenAPI latest is 3.2.1, but oapi-codegen v2.8 and openapi-typescript 7.x document support through OpenAPI 3.1; prefer OpenAPI 3.1 for the initial toolchain unless a compatibility spike proves 3.2 support.
- (version) Verified 2026-09-19: JSON Schema current dialect is Draft 2020-12, and OpenAPI 3.1 schema objects align with JSON Schema 2020-12 semantics.
- (direction) Keep a single contract authority: OpenAPI should own operations and payload schemas; do not hand-maintain separate JSON Schema definitions. Generate Go and TypeScript artifacts from the contract, and use runtime schema validation only where it serves an explicit trust or failure-handling purpose.
- (direction) React Aria Components is a strong accessibility-first candidate because it supplies unstyled composable interaction primitives with internationalization support, allowing Findur to preserve the custom Constellation visual direction; compare it against other candidates before binding.
- (direction) Adopt logical CQRS at the Findur application/API boundary: queries return purpose-built read models and writes use named intent commands. This does not yet commit to event sourcing, separate read/write databases, or asynchronous command processing.
- (version) Verified 2026-09-19: SnapTrade publishes an official OpenAPI 3.0.0 api.yaml in passiv/snaptrade-sdks; the repository currently advertises Go SDK 2.0.23 and is MIT licensed.
- (constraint) SnapTrade official api.yaml models Commercial partner-signature authentication and required Commercial fields, but the official OAuth guide says Bearer-token requests must omit those fields and warns the API reference may still show them as required. The raw vendor spec is therefore incomplete as the executable OAuth contract.
- (direction) Use the official SnapTrade Go SDK behind a Findur-owned provider port so generated SDK types, authentication quirks, retries, and vendor changes do not cross into domain modules. The SDK supports an overridable host and Bearer/OAuth token context, enabling the same adapter to target a local stub.
- (direction) Pin the upstream SnapTrade api.yaml by commit/checksum and derive a small OAuth-specific contract overlay for only the endpoints Findur uses; validate curated test fixtures against that effective contract and review upstream drift explicitly.
- (direction) Use WireMock OSS as the SnapTrade service virtualizer in integration and Docker Compose tests, covering happy paths, OAuth discovery/token lifecycle stubs, rate limits, stale/unsupported data, 401 refresh-and-retry, 425/429/503 responses, latency, and disconnect/revocation behavior. Never record live financial traffic into fixtures.
- (direction) Do not adopt Pact for the external SnapTrade boundary: without SnapTrade-run provider verification, a consumer pact cannot establish provider compatibility. Reconsider Pact only if Findur later has independently deployed internal consumers/providers; OpenAPI generation and contract tests cover the current SPA/Go monorepo boundary.
- (direction) Retain an opt-in, non-destructive live smoke suite against SnapTrade test OAuth as the final upstream compatibility check; keep it outside ordinary CI unless approved secrets are available, and redact all returned financial data from output.
- (fact) Vercel Hobby is a non-commercial personal-use envelope with finite included compute and one-hour runtime log retention; exceeding free usage can pause the project.
- (fact) Vercel Go runtime is Beta and supports net/http servers, but function storage is read-only except ephemeral /tmp; durable state must live externally.
- (decision) Candidate Vercel topology: one Vercel project and shared origin, with Vite assets deployed statically to Vercel CDN and a single internally routed Go API function under /api; do not serve SPA assets through Go.
- (fact) Vercel Hobby cron can run only once per day with hourly scheduling precision, so it cannot be the primary mechanism for timely sync, notification delivery, or outbox processing.
- (decision) Candidate persistence baseline: external managed PostgreSQL using pooled connections in the same region; avoid adding Redis or a message broker until a demonstrated requirement exists. Neon Free is a plausible fit but remains a binding decision.
- (decision) Treat webhook and request-driven work as the prompt execution paths; commit authoritative event state before best-effort external notification delivery, retaining failed work for later retry/repair.
- (rationale) Go is preferred partly for fast process initialization and low cold-start overhead in an on-demand serverless runtime.
- (fact) Vercel Pro currently costs USD 0/month before tax for one deploying seat and includes 0/month of expiring managed-infrastructure usage credit; usage beyond the credit is billed on demand and additional deploying seats cost 0/month.
- (fact) Compared with Hobby, Pro enables cron schedules as often as once per minute with per-minute precision, one-day runtime logs, configurable spend controls, collaboration, and higher configurable compute ceilings; it does not provide a durable relational database or persistent function filesystem.
- (assessment) For Findur, the meaningful Pro benefits are timely outbox/repair scheduling, improved cold-start behavior, longer diagnostics retention, commercial-use eligibility, and graceful pay-as-you-go scaling. External PostgreSQL and serverless-safe execution remain required.
- (assessment) The official Neon-Vercel integration makes Neon the leading persistence choice: it provisions managed PostgreSQL, injects environment-specific connection credentials, and can create isolated database branches for Vercel preview deployments.
- (constraint) Neon child branches normally begin with the parent branch's schema and data. Findur previews must not clone production financial data, sessions, or SnapTrade OAuth tokens; use schema-only or a dedicated sanitized/synthetic parent branch for preview databases.
- (decision) Keep database schema and versioned SQL migrations in the repository. The Neon-Vercel integration manages database resources and environment wiring, but is not the authority for application schema evolution.
- (assessment) Two supported integration modes remain to choose: Vercel-managed Neon for minimum operational setup, or a Neon-owned project linked to Vercel for clearer provider ownership and deployment portability.
- (fact) Render supports GitHub-connected automatic deploys on every commit or only after GitHub Actions checks pass. It also supports secret deploy hooks, CLI/API deployment, and repository-owned render.yaml Blueprints.
- (fact) Render Free Go web services run as normal server processes but spin down after 15 minutes without inbound traffic and take about one minute to resume; their filesystem remains ephemeral and 750 free instance-hours are shared per workspace each month.
- (fact) Render Free PostgreSQL is unsuitable for Findur durable persistence because it expires after 30 days, has no backups, and lacks managed connection pooling. Neon remains the leading database even if compute moves to Render.
- (assessment) At zero cost, Vercel static hosting plus its Go function and Neon offers a better interactive demo experience than Render Free because Render's approximately one-minute web-service wake-up is conspicuous.
- (assessment) A paid Render 0.5 CPU/512 MB Go web service is currently /month and is a strong alternative: conventional always-on net/http runtime, no platform-scale cold start, and room for DB-backed in-process outbox scheduling. Render cron jobs cost at least /month each.
- (decision) Keep the application portable across Vercel and Render: standard net/http Go entry point, external PostgreSQL, repository migrations, no durable local filesystem dependency, and Vite static output that can be hosted separately or served by Go.
- (fact) Cloudflare Tunnel can publish a homelab HTTP service at a public hostname using outbound-only encrypted connections, without opening router/firewall ingress or requiring a paid Cloudflare Access plan.
- (assessment) A Kubernetes homelab deployment is technically viable and fits the portable architecture: one Go container can serve the embedded Vite SPA and API, with cloudflared replicas routing a Cloudflare-managed hostname to a ClusterIP service.
- (decision) If homelab compute is chosen, retain Neon as the durable PostgreSQL system of record rather than making production availability, backups, and OAuth/financial-data recovery depend on homelab storage.
- (decision) Homelab delivery should use a pull-oriented GitOps path: GitHub Actions tests and publishes an immutable container image; an in-cluster reconciler deploys it. Do not expose the Kubernetes API publicly for CI deployment.
- (risk) Cloudflare Tunnel removes public-origin exposure but does not mitigate home power, ISP, router, cluster, or single-site failures. External uptime monitoring and at least two cloudflared replicas are required for a credible public demo.
- (assessment) For the active job-application/interview window, a /month always-on Render Go service is the lower-risk recommendation unless the homelab already has demonstrated reliability, GitOps, monitoring, and power/network resilience; homelab remains a viable later or showcase deployment.
- (decision) Findur will not subscribe to or depend on SnapTrade webhooks because the selected SnapTrade usage model should avoid webhook-related paid capabilities.
- (correction) Replace all earlier webhook-driven SnapTrade synchronization assumptions with pull-only synchronization: user/request-triggered reads, optional bounded polling, and persisted freshness metadata.
- (decision) Every stored SnapTrade-derived read model must record observed_at/source_sync_at and connection health; the frontend must display freshness and distinguish current, stale, refreshing, and unavailable data.
- (decision) For Real-time SnapTrade data, fetch only the account data needed for the active view and do not poll an account more often than once every ten minutes. Handle 429 responses using reset headers, exponential backoff, and jitter.
- (decision) For Daily SnapTrade data, use cached daily data by default. Do not trigger billable manual refresh implicitly; any manual refresh must be an explicit owner command with cost-aware UX, then completion is checked through account sync_status rather than a webhook.
- (assessment) Removing webhook ingress reduces the homelab availability risk: downtime delays refresh rather than losing an inbound event. A DB-backed scheduler remains useful for due refreshes and notification retries but is not required for SnapTrade event capture.
- (assessment) Recommended public deployment candidate: a /month always-on Render Go web service, Neon PostgreSQL, and a custom domain proxied through the project's own Cloudflare zone.
- (fact) Render includes managed TLS, automatic HTTPS redirects, Cloudflare-backed DDoS protection, health checks, zero-downtime deploys, instant rollback, and seven-day log retention on its Hobby workspace.
- (constraint) Render's managed edge/firewall baseline is not a customer-controlled application WAF. Use Cloudflare Free Managed Rules plus narrowly scoped custom/rate-limiting rules, and retain application-level authentication, authorization, CSRF, validation, and abuse controls.
- (decision) When Cloudflare proxies the Render custom domain, disable the default onrender.com subdomain so the Cloudflare-controlled security policy cannot be bypassed by addressing the Render hostname directly.
- (decision) Render is selected as the initial public deployment target: Hobby workspace with one paid $7/month 0.5 CPU/512 MB always-on Go web service, Neon PostgreSQL, and a Cloudflare-proxied custom domain. The application remains container-portable to the homelab.
- (correction) Persistence provider is reopened. The earlier Render recommendation did not clearly disclose that Neon is a separate provider/account and that the $7 Render web service includes no database. Render remains the selected compute candidate, but Neon versus paid Render Postgres is not yet decided.
- (correction) Render offers a separate Free PostgreSQL resource within the same Render account. It is not bundled automatically with the web service, but it can keep the initial deployment at total; the hard constraint is expiration 30 days after database creation.
- (assessment) Render Free PostgreSQL is viable for a deliberately short-lived owner-only portfolio demonstration if its 30-day lifetime, lack of backups, and possible reauthorization/reseeding on loss are explicitly accepted. Create it only when the public demonstration is ready to launch, not during development.
- (decision) Use local/containerized PostgreSQL during development and CI so the Render Free PostgreSQL 30-day clock is not started prematurely. Before public launch, choose either a Free Render database with an expiry runbook or a durable provider/paid upgrade.
- (decision) Use Render Free PostgreSQL for the public 30-day Findur demonstration. Create it only at launch; accept its 1 GB limit, lack of backups and managed pooling, and scheduled expiry as deliberate constraints. Use containerized PostgreSQL for local development and CI, and retain an upgrade/migration path if the demonstration must remain live beyond 30 days.
- (correction) Render Static Sites are a first-class free global-CDN capability with automatic Git deploys, PR previews, managed TLS, DDoS protection, Brotli, HTTP/2, custom headers, and SPA rewrites. The earlier embedded-SPA recommendation underused the selected platform.
- (decision) Revised Render topology: deploy React/Vite as a free Render Static Site, deploy the Go backend as the always-on Render Web Service, and use Render Free PostgreSQL for the 30-day launch window.
- (decision) Keep frontend and API independently buildable within one monorepo and one render.yaml Blueprint. The API contract remains the integration boundary; deployment coordination waits for CI checks and contract tests.
- (decision) Adopt a zero-cost development deployment: free Render Static Site, free Render Go Web Service, and disposable development data. Accept the free API service's idle spin-down and approximately one-minute wake-up during development.
- (decision) Upgrade the existing Render Go Web Service to the always-on plan only when the public job-search demonstration launches.
- (decision) Keep the public 30-day database window separate from development. Use local/CI PostgreSQL as the primary development store; if a Render Free development database is used, treat it as disposable and create a fresh production database at launch with repository migrations and synthetic seeds before introducing live OAuth state.
- (decision) CI/CD is the first vertical slice, not a launch-phase task. Establish GitHub Actions and free Render deployments as soon as the repository skeleton exists, so every main-branch change exercises the real delivery path.
- (decision) GitHub Actions is the authoritative CI gate. Configure Render Git-connected services with autoDeployTrigger checksPass so Render deploys only after all GitHub Actions checks succeed; prefer this over storing and invoking Render deploy hooks from GitHub.
- (decision) Initial CI gates cover OpenAPI validation and generated-code drift, Go format/vet/test/build, frontend typecheck/lint/test/build, and integration tests using ephemeral PostgreSQL plus WireMock. Add browser smoke tests once the first user journey exists.
- (constraint) Render pre-deploy commands are unavailable on a free web service. Use one owned migration executable: during free development the Go service runs migrations under a PostgreSQL advisory lock before becoming ready; after upgrading the launch service to the paid tier, invoke the same executable as Render's pre-deploy command.
- (decision) Database migrations must follow expand-contract compatibility so independently deployed old/new frontend and backend versions remain interoperable. GitHub Actions will not receive production database credentials or run production migrations.
- (decision) Commit a render.yaml Blueprint without secrets. Define the free static site and free Go web service immediately, use monorepo root directories/build filters, and keep launch as a configuration promotion: upgrade the Go service and attach a fresh 30-day free production PostgreSQL database.
- (decision) Use github.com/golang-migrate/migrate/v4 as the database migration engine. Keep ordinary schema migrations as versioned PostgreSQL .up.sql/.down.sql files, embedded or packaged with an owned Go migration command; reserve Go-coded migrations for exceptional transformations.
- (decision) Production schema rollback is normally roll-forward plus application rollback against an expand-contract-compatible schema; do not automatically run down migrations during deployment. Test both directions locally where safe.
- (decision) Manage Render infrastructure as code through a root render.yaml Blueprint. It will define the React static site, Go web service, free PostgreSQL database, build/start/health settings, build filters, and checksPass deployment gates. Reference the database connection dynamically and mark manually supplied secrets sync:false; never commit secret values.
- (fact) Render Blueprint changes sync infrastructure configuration from the repository. Dashboard changes that conflict with Blueprint fields can be overwritten on the next sync, so render.yaml is the source of truth for managed settings.
- (decision) Build observability around OpenTelemetry APIs and semantic conventions, with Datadog as the first backend. Keep domain/application instrumentation vendor-neutral and isolate exporter/bootstrap configuration at the application edge.
- (decision) On the initial Render Hobby deployment, send application traces and custom metrics from the Go service to Datadog's direct OTLP HTTPS intake because running an Agent or Collector is disproportionate. Make exporters, sampling, endpoint, site, environment, service, and version configurable; telemetry failure must never fail a user request.
- (decision) Write structured JSON application logs to stdout/stderr and configure Render's workspace log stream to forward them to Datadog over HTTPS. Include trace_id and span_id for correlation. Do not also export the same application logs via OTLP, avoiding duplicate ingestion.
- (constraint) Render provides dashboard logs and basic platform/service/database metrics, with seven-day Hobby log retention, but application tracing is still our responsibility. Render metrics streaming to Datadog requires a Pro workspace and is not unlocked merely by the paid web-service instance; defer it and use Render's dashboard for platform metrics.
- (decision) Telemetry has a strict safe-schema allowlist: never record request or response bodies, authorization/cookie headers, OAuth material, SnapTrade payloads, holdings, balances, account identifiers, job-application contents, emails, or raw URLs/query strings. Use route templates, bounded error classifications, and low-cardinality attributes.
- (decision) Instrument inbound HTTP, PostgreSQL calls, outbound SnapTrade HTTP, refresh workflows, migrations, and command/query handlers. Standard resource tags are service.name, deployment.environment, service.version/commit, and service.instance.id; propagate W3C trace context.
- (decision) Make telemetry disabled/no-op by default for local development and tests unless explicitly enabled. Use batching, timeouts, sampling, and graceful shutdown/flush; exporter failures are logged in a bounded way and never retried on the request path.
- (open_question) Frontend Datadog RUM is separate from backend observability. If adopted, use the browser client token, disable Session Replay, avoid user identity and financial data, scrub URLs/actions/errors, and place collection behind the product's analytics/privacy decision.
- (correction) Render offers an official Datadog Agent template that runs as a separate private service and receives APM traces and DogStatsD metrics from application services. This is not tracing automatically included with the Go web-service instance.
- (decision) Defer deploying a Datadog Agent, direct OTLP exporter, Render log stream, and frontend RUM for the initial MVP. Preserve the capability through safe structured JSON logs, trace/request correlation fields, and a single observability bootstrap boundary so OpenTelemetry/Datadog can be enabled later without touching domain code.
- (decision) Initial production observability uses Render's built-in logs and metrics. Telemetry integrations remain optional configuration and must not become a launch dependency.
- (correction) Use Render-native observability only for the initial product. Remove Datadog, OpenTelemetry exporters, the Render Datadog Agent, log streams, RUM, and telemetry abstraction work from MVP scope.
- (decision) The Go service writes redacted structured JSON logs to stdout/stderr using the standard library log/slog, including an application-generated or propagated request ID. Render captures these logs; the app emits request completion summaries because Render public HTTP request logs require a Pro workspace.
- (decision) Use Render's built-in service and PostgreSQL metrics dashboards, deploy/event logs, health checks, and Hobby seven-day log retention. No external observability service is a launch dependency; reassess only if the product outgrows these facilities.
- (decision) Do not design around or compensate for Render's Pro-only HTTP access logs. Use Render's seven-day Hobby retention for application/runtime logs; emit only useful safe operational and error logs from the Go service.
- (decision) OpenAPI 3.1 is the sole authored contract for Findur's HTTP API. Its component schemas define request and response data using the OpenAPI 3.1 JSON Schema dialect; do not maintain parallel standalone JSON Schema contracts for the same payloads.
- (decision) Generate Go API/server types and TypeScript client/types from OpenAPI. Where runtime validation is needed, generate or configure validators from the same OpenAPI schemas; TypeScript types alone are compile-time only. Domain invariants remain explicit Go domain logic rather than schema rules.
- (decision) Drop strict JSON:API. Use ordinary JSON with intent-oriented command endpoints, purpose-built query endpoints, and a consistent error representation documented in OpenAPI.
- (constraint) Standalone JSON Schema is appropriate only for a future non-HTTP document contract such as configuration, stored user-authored documents, or asynchronous messages. If introduced, it must have a distinct ownership boundary and must not duplicate the OpenAPI payload schemas.
- (decision) The pinned SnapTrade OpenAPI document is a separate upstream contract used to generate or verify the provider adapter/client; it does not become part of Findur's public OpenAPI contract.
- (correction) Do not characterize Findur as CQRS. Most owner editing is coarse-grained or bulk saving of resources, so a command/query architecture, command bus, handler taxonomy, or separate read/write models would add ceremony without a demonstrated need.
- (decision) Use a conventional resource-oriented HTTP API and application-service layer. Prefer GET for reads and PUT/PATCH for owner-managed resources such as profile, portfolio presentation, disclosure policy, and application content; make bulk saves atomic database transactions.
- (decision) Represent genuinely asynchronous or process-like operations as resources, for example POST /syncs followed by GET /syncs/{id}, without calling the overall system CQRS. Event sourcing and separate read/write databases are out of scope.
- (decision) Protect coarse-grained saves from lost updates using a persisted revision/version exposed by the API and conditional writes such as If-Match or an equivalent expectedRevision field; return a conflict response instead of silently overwriting newer edits.
- (fact) SnapTrade publishes an OpenAPI 3.0.0 api.yaml in the official passiv/snaptrade-sdks repository. It describes the required portfolio endpoints and response schemas, but its global security and endpoint parameters still model Commercial HMAC authentication with clientId/timestamp/signature and required userId/userSecret.
- (fact) SnapTrade's official OAuth guide explicitly requires Bearer access tokens and says OAuth requests must omit clientId, consumerKey, userId, userSecret, timestamp, and Signature, even where the API reference still shows Commercial fields as required. Therefore the unmodified vendor OpenAPI cannot safely generate Findur's OAuth client.
- (proposed) Preferred SnapTrade client approach: commit an unchanged pinned upstream api.yaml plus source commit/checksum, apply a small deterministic OAuth-specific overlay to produce a derived generation spec for only approved read/connection operations, and generate the Go client/types from that derived spec. Keep OAuth discovery/token/ID-token handling in standard Go OAuth/OIDC libraries and map generated vendor types through an owned adapter.
- (proposed) CI should validate the pinned SnapTrade spec and overlay, regenerate the client with a pinned generator, fail on generated-code drift, and run WireMock contract scenarios for OAuth bearer headers, absence of Commercial auth fields, schemas, pagination, 401 refresh-once behavior, 429 Retry-After/backoff, partial account failures, and provider error mapping.
- (decision) Use the official OpenAPI Overlay Specification rather than directly editing the vendored SnapTrade api.yaml or using an ad hoc runtime transport workaround. Keep the upstream specification byte-for-byte unchanged and pinned to an upstream commit/checksum.
- (decision) The SnapTrade OAuth overlay is a small generation-time compatibility patch justified by the provider's documented mismatch: the OAuth guide requires Bearer-only requests while the published OpenAPI and generated Go SDK still model Commercial authentication. The overlay adds bearer security, overrides security only for an allowlist of OAuth-supported operations, and removes Commercial-only userId/userSecret parameters.
- (decision) Treat the vendored SnapTrade spec plus the OAuth overlay as the source inputs. Produce a derived OAuth OpenAPI document and generated Go client deterministically with pinned tooling. CI validates the output, asserts OAuth invariants and expected overlay matches, and fails on generated-code drift.
- (constraint) The overlay must remain narrow and documented. If it expands beyond authentication-shape corrections or starts redefining response schemas and behavior, stop and reassess with SnapTrade rather than maintaining a shadow API specification.
- (decision) The derived SnapTrade OAuth OpenAPI document drives provider contract testing: it generates the Go client and defines the schemas against which WireMock request/response fixtures are validated.
- (decision) Use WireMock OSS locally and in CI under Docker Compose. Author a small set of explicit scenario mappings and fixtures for behavior that OpenAPI does not express, including OAuth token exchange/rotation, 401 refresh-once, 429 Retry-After, disabled connections, partial account failures, empty/unsupported datasets, and malformed or changed provider responses.
- (constraint) Do not treat WireMock mappings as an independent contract. Each successful SnapTrade fixture must identify its operationId/status/content type and pass automated validation against the derived OAuth OpenAPI response schema; integration tests also assert Bearer authentication and absence of Commercial auth fields.
- (decision) Do not use Pact for the external SnapTrade boundary because SnapTrade does not publish or verify a Pact provider contract. The pinned upstream OpenAPI plus overlay, generated client, schema-validated fixtures, WireMock scenarios, and an optional non-destructive live smoke test provide the appropriate assurance.
- (constraint) SnapTrade OAuth authorization-server endpoints and OIDC discovery are not covered by the portfolio api.yaml. Mock their documented discovery, authorization callback, token, JWKS, refresh, and revocation behavior separately, using standard protocol fixtures and validation.
- (decision) Apply strict data minimization to the SnapTrade integration: every identity claim, OAuth scope, endpoint call, source field, persisted attribute, client response, cache, and log field must map to a named product requirement. Data without a current requirement is not collected.
- (decision) Initial SnapTrade OAuth scopes are openid and read only. SnapTrade requires read, and openid supplies the verified stable sub used for Findur identity. Do not request profile, email, trade, or webhook because current requirements do not need them.
- (decision) Before Account Inclusion is confirmed, call only what is necessary to enumerate eligible accounts and present minimal selection labels. Do not retrieve balances, holdings, positions, activities, or orders before inclusion. No account is included by default.
- (decision) After Account Inclusion, retrieve only selected accounts and only the datasets and fields required for the private Portfolio Showcase, defined derivations, matching, freshness, and disclosure previews. Prefer immediate normalization and purpose-tagged persisted snapshots; do not persist or log complete raw SnapTrade payloads without a separately justified requirement.
- (decision) Findur profile fields such as display name, photo, biography, adult confirmation, and preferences are collected directly by Findur rather than requesting SnapTrade profile or email claims. Disconnect revokes provider access and purges locally held source and derived financial data as required by the PRD.
- (correction) Findur is multi-user: each verified SnapTrade OIDC sub maps to an independent Findur user, session, authorization, included accounts, snapshots, and preferences; do not assume a singleton owner allowlist.
- (decision) Serialize rotating OAuth refresh tokens per Findur user using a PostgreSQL row or advisory lock, atomically replace both tokens, retry a provider request once after 401, and mark reauthorization_required on invalid grant. OAuth token refresh is distinct from portfolio-data refresh.
- (fact) SnapTrade OAuth bearer authorization and API-key authorization may coexist in one backend, but they are separate credential and user/connection ownership domains. API-key credentials cannot supplement or replace the bearer token for an OAuth-authorized Personal user.
- (decision) The assignment explicitly requires following SnapTrade OAuth Apps documentation. Findur will implement OAuth/OIDC bearer authorization as its sole product integration path; Personal or Commercial API-key authentication is out of scope and will not be implemented as a fallback or alternate onboarding flow.
- (correction) Keep OIDC identity, OAuth provider authorization, and Findur session distinct. SnapTrade id_token is a short-lived JWT authentication assertion verified only during authorization callback and then discarded; it is not OAuth state and is not refreshed. OAuth state is random one-time callback correlation/CSRF material. Findur creates its own session. SnapTrade access tokens are refreshed shortly before their 10-hour expiry using rotating refresh tokens, with per-user serialization only to prevent concurrent rotation races.
- (decision) Keep the Go web tier stateless across Render instances. After verifying and discarding the SnapTrade OIDC id_token, Findur issues its own short-lived signed session JWT in a Secure HttpOnly cookie. Do not persist ordinary browser sessions or expose SnapTrade tokens to the browser. Durable OAuth grants, encrypted provider tokens, user data, and cross-instance refresh coordination remain in PostgreSQL; stateless web tier does not mean a stateless system.
- (correction) The OAuth authorization code is strictly one-time. The OIDC id_token is a signed login assertion verified at callback and then discarded by Findur; it is not used for SnapTrade API calls. The code exchange also returns a reusable OAuth access_token, sent as Authorization Bearer on SnapTrade calls for up to 10 hours, and a rotating refresh_token used server-side to obtain replacement access and refresh tokens.
- (decision) Replace the Findur-issued session JWT decision with PostgreSQL-backed server sessions. After validating the SnapTrade OIDC id_token, create a cryptographically random opaque session identifier, store only its hash with user_id and expiry, and send the identifier in a Secure HttpOnly SameSite cookie. Render instances share PostgreSQL, so no sticky sessions or Redis are required. SnapTrade access and refresh tokens remain separate encrypted server-side credentials.
- (principle) Prefer mature, actively maintained, community battle-tested packages for protocol-heavy and security-sensitive concerns. Do not hand-roll OAuth/OIDC validation, PKCE, secure sessions/cookies, cryptography, OpenAPI parsing/code generation, database migrations, or retry/backoff behavior. Pin versions, wrap dependencies behind narrow application interfaces, and add integration tests at those boundaries.
- (decision) Wrap the SnapTrade OAuth client in a resilience transport using mature packages: golang.org/x/time/rate for proactive admission control and a maintained circuit-breaker library such as sony/gobreaker/v2. Parse customer X-RateLimit-Limit/Remaining/Reset and Personal-account X-RateLimit-Account-Limit/Remaining/Reset headers; on 429 pause only the exhausted bucket and retry safe reads with bounded jitter. Do not count 429, 401, or ordinary 4xx responses as breaker failures. Count transport errors, timeouts, and 5xx; when open, fail fast and serve freshness-labelled persisted snapshots. Preserve X-Request-ID in safe structured logs. Never blindly retry rotating OAuth token exchanges, billable manual refreshes, or other non-idempotent operations.
- (decision) Candidate-pool eligibility requires an explicit opt-in plus a live presence lease; being online alone is not disclosure consent. The SPA renews a short presence lease while actively used, and the user leaves the pool automatically when the lease expires. Any active session may renew the user-level lease.
- (decision) Use activity-driven portfolio refresh with persisted snapshots. On transition to active, or when an active user crosses the freshness threshold, atomically claim one refresh per user/account and refresh in the background. Serve last-known freshness-labelled snapshots immediately; do not call SnapTrade from heartbeats or candidate-view requests. Stop scheduling refreshes after presence expires. On Real-time, never poll more often than SnapTrade recommendation of once per 10 minutes per account. On Daily, do not automatically invoke potentially billable manual refresh.
- (question) User language now suggests real signed-in people appear in one another candidate pools, contradicting the current synthetic-candidate PRD boundary. Confirm whether real-user discovery replaces or supplements synthetic candidates; this changes privacy, consent, and SnapTrade production-compliance scope.
- (decision) For OAuth-authorized Personal users, do not assume one global Real-time or Daily mode from Findur configuration. Record each connection data_freshness_mode and account sync_status returned by SnapTrade, treat the effective mode as delayed if either institution or SnapTrade reports delayed, and apply activity-driven refresh policy accordingly. Do not invoke the billable manual-refresh endpoint automatically; support only endpoints verified as OAuth bearer-compatible in the derived allowlisted spec.
- (correction) The generated brief, PRD, reconciliation, and UX artifacts repeatedly impose an owner-only live portfolio plus synthetic-only Discovery boundary, but the user states they never specified no cross-user behavior. Treat that boundary as an erroneous/inferred planning constraint requiring upstream correction. Architecture intent is multi-user: independent concurrent SnapTrade OAuth users may enter the real candidate pool while actively online, subject to explicit inclusion/disclosure consent and production/compliance constraints. Do not use the synthetic-only clauses to override this direct user correction.
- (correction) Findur uses a hybrid candidate population. Real eligible OAuth-authenticated users who are currently online may appear to one another. A generated synthetic population fills inventory when insufficient real users are available so Discovery remains demonstrable. Synthetic candidates are fallback/demo records, not the exclusive pool; they have explicit provenance, no OAuth credentials or real financial data, and deterministic simulated swipe outcomes. Real candidates and real reciprocal swipes remain first-class.
- (decision) Control synthetic candidates with validated startup configuration. SYNTHETIC_CANDIDATES_ENABLED determines whether the synthetic candidate source participates; when false, Discovery is real-only and honestly shows low/no inventory. When true, eligible real online candidates remain first-class and synthetics fill configured deck inventory. Disabling does not delete generated fixtures. Enforce provenance and the toggle centrally in candidate assembly, expose the active non-secret setting in safe diagnostics, and test both modes.
- (decision) Refine hybrid candidate assembly: eligible online real users always take precedence. Synthetic candidates are added only when SYNTHETIC_CANDIDATES_ENABLED is true and real inventory is below a configurable minimum deck target; fill only the shortfall. If enough real candidates exist, include zero synthetics automatically. If synthetics are disabled, show an honest low/no-inventory state.
- (correction) Remove SYNTHETIC_CANDIDATES_ENABLED. Candidate assembly always selects eligible online real users first, then adds only enough synthetic candidates to reach the minimum deck-depth target. When real inventory meets the target, no synthetics appear. The automatic shortfall rule replaces an explicit on/off switch.
- (correction) Findur will not be deployed as a production dating service. It is a publicly hosted assignment/evaluation demonstration using SnapTrade test OAuth, supporting the small allowed set of concurrent real test users plus synthetic inventory fallback. Do not treat production KYC, commercial approval, public-launch legal review, scaled moderation, or production operations as architecture blockers. Preserve reasonable security, consent, disclosure, and data-handling hygiene because test users may authorize real financial data.
- (constraint) Non-production status removes production launch/KYC/review gates but does not waive SnapTrade Developer Terms. Current terms prohibit redistributing Market Data and require express consent from both the End User and SnapTrade before mining, re-selling, or re-packaging End User data to third parties. Cross-user display of real holdings, values, or derived portfolio profiles is therefore not clearly safe merely because this is a demo. Safest evaluation architecture: real concurrent OAuth test users use SnapTrade Sandbox/test brokerage data for cross-user candidate experiences; any actual brokerage data remains private unless SnapTrade gives written confirmation.
- (decision) Evaluation environment contract: Findur uses the SnapTrade Test OAuth app for up to five concurrent SnapTrade Personal test users. Each participating test user connects and includes only SnapTrade Sandbox brokerage accounts, so cross-user Discovery, disclosure, swiping, and matching operate on simulated provider data rather than real brokerage data. Generated synthetic candidates continue to fill candidate-inventory shortfalls.
- (correction) SnapTrade does not document pre-provisioned OAuth test identities. The Test OAuth app accepts up to five distinct ordinary SnapTrade Personal accounts; each human tester creates or uses a Personal account, connects the Sandbox brokerage, and authorizes Findur. Do not call Commercial registerUser. Use WireMock/local identity-provider fixtures for automated CI and reserve real Personal-account OAuth for manual smoke testing. If controlled additional Personal identities are needed, confirm permitted setup with SnapTrade rather than assuming dummy accounts exist.
- (correction) Using the Test OAuth app and Sandbox brokerage avoids exposing real financial records but is not documented as a blanket waiver of SnapTrade data-use terms. Production compliance review is deferred, while limitations on redistributing Market Data and repackaging End User data apply during testing too. Cross-user display of SnapTrade-returned Sandbox portfolio fields is therefore low-risk but not expressly authorized in published terms. Generated Findur synthetic portfolio data is the zero-ambiguity cross-user fallback; obtain written SnapTrade confirmation before treating provider-returned Sandbox fields as freely redistributable among testers.
- (decision) Cross-user display of SnapTrade-returned portfolio data is not important enough to justify terms ambiguity. Keep every user SnapTrade Sandbox source data and derived live preview private. Real users still authenticate, maintain online presence, appear as candidates, swipe, and match; any portfolio data shown on another user candidate card or used for cross-user demonstration matching is generated Findur synthetic data and clearly labelled as such. Fully synthetic candidate records fill real-user inventory shortfalls.
- (decision) Assign every real authenticated Findur user a stable generated synthetic candidate portfolio. Candidate-facing portfolio cards and cross-user matching representations always use synthetic portfolio data, regardless of whether the candidate identity is real or generated. A user own SnapTrade Sandbox data remains private and may be used only to serve that user, including private showcase and personalization/ranking against candidate synthetic portfolios. Model identity provenance and portfolio provenance separately; label demo portfolio data clearly. Fully synthetic identities continue to fill inventory shortfalls.
- (correction) Replace the hybrid real/synthetic candidate model. Real OAuth-authenticated users may use Findur concurrently but are never served to one another as candidates. Discovery contains only generated synthetic candidates, personalized for each real user using that user private permitted portfolio inputs and preferences. Synthetic candidates have deterministic simulated swipe outcomes. Remove real-user presence leases, real-candidate inventory precedence/fill logic, cross-user portfolio presentation, and real reciprocal-match coordination from the evaluation architecture. The existing PRD synthetic-Discovery boundary remains valid; only its single-owner assumption must be corrected to support multiple independent test users.
- (decision) Use one shared, deterministically generated synthetic user population. Synthetic identities use the same candidate/profile/portfolio/disclosure schema and stable IDs as real users, distinguished by user kind/provenance; they have no login, session, or OAuth authorization. Each real viewer receives a per-user deck projection over the shared population: eligibility, personalized ranking/order, swipe decisions, and simulated match state are viewer-scoped. Do not duplicate synthetic records per real user or mutate global synthetic state when a viewer swipes.
- (decision) Synthetic candidates are normal Findur user records created by the deterministic population generator rather than the SnapTrade OAuth callback. Reuse the same user, profile, portfolio, disclosure, candidate, swipe, and match domain structures. Only OAuth-created real users have external SnapTrade identity, authorization, token, and session records. Retain a small explicit origin/provenance field rather than inferring synthetic status solely from a missing authorization, because a real user may disconnect.
- (decision) Candidate eligibility, cohorting, ranking, disclosure, card rendering, swipes, and matches operate on one normalized Findur user/profile/portfolio model and do not branch on portfolio source. Generator-created users must satisfy the same profile completeness, usable-portfolio, disclosure, proximity, activity/presence, prior-decision, and safety rules as OAuth-created users. Source provenance changes labels, refresh/lifecycle behavior, and authentication capability only; it does not bypass product rules or create a second candidate engine.
- (decision) Persist only product-required fields from SnapTrade after validation and normalization; never persist or log complete raw provider response bodies. Refresh outside the database transaction, then atomically publish each successfully normalized dataset snapshot and its derived signals with source/version/freshness references. Preserve the last trustworthy snapshot when refresh fails. Apply the same purpose-limited normalized schema to the shared generated user population.
- (correction) Do not persist a full discovery_deck/deck_items order. Discovery is a dynamic projection over the shared generated users. On each next-card request, evaluate current eligibility and ranking from the viewer latest inputs and candidate current state, excluding persisted swipes/impressions. Persist only durable interactions plus a lightweight current-candidate/navigation anchor for safe detail/back restoration. Keep the active card stable until decision or invalidation; recompute subsequent candidates dynamically with deterministic tie-breaking.
- (decision) Simulate synthetic-user presence by randomly rotating members of the shared generated population online and offline. Store presence in the same user-presence model consumed by candidate eligibility; viewers observe one shared state, not per-request randomness. Use a seeded deterministic simulator with controllable clock for repeatable tests, scheduled transition times and varied online durations, plus a minimum online inventory floor so Discovery remains usable. Candidate queries only serve users online at selection time; an already-issued card receives a short offer lease to avoid disruptive flicker during interaction.
- (decision) Add a seeded synthetic-portfolio simulator. At scheduled intervals, select a subset of generated users and apply coherent synthetic events such as price/value movement, deposit/withdrawal, trade/order/activity, or rebalance, producing a new normalized portfolio snapshot and recalculated derived signals. Never randomize dependent fields independently: balances, quantities, prices, values, allocations, currencies, orders, and activities must reconcile and satisfy generator invariants. Use no live market data. Publish each update atomically, retain snapshot version for any active-card offer lease, and let subsequent candidate rankings reflect the new version.
- (decision) Run synthetic presence and portfolio evolution as a lightweight periodic job inside every Go web-service instance. Each tick opens a short PostgreSQL transaction and attempts a fixed transaction-scoped advisory lock; only the winner reads/advances durable simulation schedule state, applies a bounded deterministic batch, records the next due time, and commits. Lock release is automatic on commit/crash. Losing instances skip the tick. After Render sleep/restart, coalesce missed time into current state rather than replaying every interval. Use a standard context-aware ticker/clock abstraction, not a separate worker, queue, or cron service.
- (decision) Synthetic presence rotation and coherent synthetic portfolio evolution run as one periodic simulation tick inside the Go service. Every service instance may wake, but only the instance that obtains a fixed PostgreSQL pg_try_advisory_xact_lock advances the durable simulation schedule and applies one bounded deterministic batch in the same short transaction; losers skip. Missed intervals are coalesced after sleep/restart. Use pgx/v5 directly for the transaction-scoped lock; reviewed Go lock/scheduler packages add lease/session semantics or job-system machinery that this single database-only task does not need.
- (decision) PostgreSQL access uses native github.com/jackc/pgx/v5 with pgxpool. All parameterized application/repository SQL uses descriptive @name placeholders with pgx.StrictNamedArgs (or the corresponding strict struct form where appropriate); positional $1/$2 parameters are not authored in repository SQL. Migrations, static/parameterless DDL, and generated SQL are exempt where named runtime parameters do not apply. Do not add sqlx solely for named parameters because pgx natively provides them.
- (correction) Separate SnapTrade OAuth login from the generated Go API SDK. The OAuth-app user login is authorization-code + PKCE/OIDC and is implemented with maintained Go OAuth/OIDC libraries using SnapTrade discovery metadata. Do not call the SDK AuthenticationApi.LoginSnapTradeUser method: that generates a Connection Portal URL for the separate Commercial userId/userSecret integration. The SDK SetOAuthClientCredentials method is the OAuth client-credentials grant and is likewise not the user authorization-code flow. After login, prefer the official SDK for bearer-authenticated data calls, subject to an integration test proving it emits the documented OAuth request without Commercial parameters or signing.
- (correction) The visible ARCHITECTURE-SPINE.md is now the primary review surface and has been populated from the settled coaching decisions. The hidden memlog remains provenance/reasoning support; future settled decisions must be promoted into the spine promptly rather than accumulating only in the memlog.
- (decision) Keep the React frontend as a Render Static Site while preserving one browser origin: order Static Site routes so /api/* rewrites to the Go Web Service before the /* to /index.html SPA fallback. Use the public static-site /api OAuth callback URL. This retains CDN/static benefits and first-party session cookies without permissive cross-origin session handling.
- (decision) Encrypt SnapTrade access and refresh tokens independently with AES-256-GCM using fresh nonces, stable associated data binding owner/provider/token-kind/version, and a Render-secret versioned key ring. Store envelope metadata, expiry, scopes, and row version. Serialize rotating refresh tokens per authorization with a bounded PostgreSQL row lock; atomically replace token pair and metadata. A post-provider/pre-commit failure requires reauthorization rather than unsafe token replay.
- (correction) Use the official SnapTrade Go SDK behind a narrow provider adapter by default. Before broad use, prove bearer-only OAuth request shape and omission of Commercial auth fields in a request-capture contract test. A minimal audited adapter is the first fallback; maintaining a generated overlay client is now last resort, not the default plan.
- (decision) SnapTrade OAuth details are explicit in the visible spine: discover authorization/token/revocation/JWKS from the OIDC metadata; cache metadata/JWKS and refetch unknown kid; exact HTTPS deployed redirect and supported HTTP loopback only; expiring session-bound single-use state/PKCE/nonce attempt; token and revocation client authentication via HTTP Basic with form bodies; no userinfo call; verify RS256/iss/aud/exp/iat/nonce; persist sub and discard ID token; record actual scope and 10-hour access expiry; refresh responses have no ID token; revoke refresh token on disconnect then delete local state even if revocation fails.
- (correction) SnapTrade explicitly lists its official Go SDK for the OAuth integration path; treat that as authoritative provider intent. The architecture now mandates the official SDK rather than framing it as a compatibility candidate. Bearer request-shape coverage is routine integration and upgrade verification. Do not fork, patch, overlay, regenerate, or replace the SDK unless a minimal reproduction demonstrates a defect and SnapTrade confirms the supported configuration or need for a workaround.
- (decision) Normalized accounts/balances/positions/activities are published as immutable dataset-specific snapshot versions with an atomic current pointer and dataset-specific provider observation/retrieval/freshness metadata. Retain an older version only while an active card or preview anchor references it or during a short bounded cleanup window; then delete it. Do not retain raw payloads or indefinite financial history. OAuth and generated users share this snapshot structure.
- (decision) Represent money, quantities, prices, cost basis, weights, and percentages with PostgreSQL numeric and shopspring/decimal in Go, using jackc/pgx-shopspring-decimal on every pooled connection. OpenAPI exposes decimal values as strings; monetary values always include ISO-4217 currency. Preserve provider precision through normalization, round only under an explicit calculation/display rule, and never aggregate currencies without an explicit freshness-labelled FX model.
- (clarification) Do not store all financial values as integer cents. Currencies have different minor-unit scales, and fractional security units, crypto quantities, prices, and cost bases need additional precision. Canonical storage remains decimal numeric plus explicit currency where monetary; conversion to currency-specific minor units is allowed only at a calculation/display boundary that defines it.
- (DECISION) User delegated relational schema choices to the architect. Added DATA-MODEL.md as a visible companion covering identity and sessions, encrypted OAuth authorization state, profiles and preferences, normalized immutable provider snapshots with atomic heads, signal lineage, synthetic state, discovery anchors, swipes and matches, deletion/retention, numeric types, constraints, and concurrency rules. Future schema choices may be made without interrupting the user unless they materially affect product behavior or infrastructure cost.
- (DECISION) The first cut has no profile image uploads. Profiles use only allowlisted application-owned placeholder avatar assets bundled with the frontend; the API stores/returns an avatar key rather than an arbitrary URL. No media table, binary image storage, object storage, image processing, or upload endpoint is in scope.
- (DECISION) Reconciled notifications with the synthetic-only candidate model: supersede PRD FR-28 for the evaluation first cut. Browser Web Push, VAPID, notification preferences, installed-app badging, delivery outbox, and persistent notification feed are out of scope because generated interest and reciprocal outcomes arise only while a real user is active in Discovery. Model swipes as viewer-specific directed real/generated decisions; generated-to-real positive swipes can produce an in-session Incoming Interest, and mutual outcomes are persisted atomically for immediate/restorable in-app presentation.
- (DECISION) Proximity uses a fixed seeded catalogue of coarse city/region areas shared by OAuth and generated profiles. Users select an area; Findur does not request device/browser location, infer from IP, accept addresses/postal codes, call a geocoder, or require PostGIS. Server-only public centroids feed one tested Haversine function; maximum distance is a positive bounded integer in kilometres with an inclusive boundary. APIs expose at most an allowed coarse label and approximate distance band, never coordinates or exact calculated distance. Area or distance changes invalidate the current candidate anchor.
- (DECISION) Constrained the first-cut compatibility model to a direct, purpose-limited SnapTrade projection. Allowlisted inputs are accounts (identity/category/total/currency/sync), positions/all (instrument kind, units, price, currency, cash-equivalent and only display-needed cost basis), balances (cash/buying power for display), and a bounded recent activities page (type/recency and disclosure-needed fields). Ranking uses only position-derived asset-kind mix, breadth, and largest-position concentration: similar=min allocation difference; diversified=broader kind coverage then lower concentration; complementary=lower overlap; approximate proximity is secondary and stable candidate ID breaks ties. Cash/activity are display bands only. No orders, returns, tax lots, quotes, external market/FX/sector enrichment, ML, or invented missing values. Same versioned builder handles OAuth and generated snapshots with explicit coverage.
- (DECISION) Coherence/scope pass: removed accidental synthetic-order references, replaced generic scoring/weight language with the adopted deterministic comparisons, bounded activity retrieval to the approved recent display window, and corrected OAuth initiation to use a separate one-time pre-login cookie rather than a nonexistent authenticated session. The callback expires that cookie before issuing the SameSite=Lax Findur session. Added an explicit current signal head and made candidate anchors pin both viewer/candidate signal sets plus disclosure version so immutable snapshot retention and safe Detail/Back restoration are implementable. Deferred diagrams now exclude the completed relational ownership spine.
- (DECISION) All multi-repository mutations use an application-owned explicit UnitOfWork/transaction scope. The callback receives repositories bound to the same pgx.Tx and passes them explicitly; repositories never begin/commit/rollback and context never hides a transaction. Domain code is persistence-agnostic. No provider/OAuth/network/filesystem/sleep or unbounded work runs inside a DB transaction; use prepare/call/finalize with short version-checked transactions. Replaced refresh row-lock-across-network with a short per-authorization refresh lease claim, out-of-transaction token call, and short CAS publication. Ambiguous refresh outcomes fail closed to reauthorization and are never blindly retried. Integration tests must prove cross-repository propagation, rollback, and no open transaction during delayed provider calls.
- (DECISION) Generate the HTTP transport edge from the single OpenAPI 3.1 contract. Pin oapi-codegen v2.8.x to generate Go models, stdlib net/http routing glue, and StrictServerInterface, with its nethttp request-validation middleware. A handwritten adapter implements that interface, maps generated DTOs, and delegates product operations to application services; handlers never call repositories/providers and generated DTOs never enter the domain. Generate browser types with openapi-typescript 7.x and use openapi-fetch 0.x. Generated files are not hand-edited and CI fails on regeneration drift.
- (DECISION) Input reconciliation tightened existing scope: explicitly supersede the one-owner assumption in favor of up to five isolated Test OAuth subjects; treat all Photos as bundled placeholders; and supersede issuer/balancing-exposure matching interpretations with the adopted asset-kind model. Before Account Inclusion fetch only minimum inventory metadata. Bulk inclusion uses prepare/call/finalize: removals commit and invalidate immediately, provider fetches for additions occur outside transactions, and additions activate only with atomic snapshot/signal publication. Financial dataset versions/heads are per account while inventory is portfolio scoped, so consent deletion removes whole immutable versions. Disclosure downgrade invalidates anchors/projections fail-closed. Added explicit per-dataset freshness thresholds, disabled-connection Dashboard recovery distinct from OAuth reauth, deterministic initial synthetic generation, server-side disclosure DTO projection, protected no-store responses, manifest-without-service-worker cache policy, locale/theme ownership, profile completeness fields, duplicate-account fingerprints, date-only activity semantics, and precision wording bounded by the SDK.
- (decision) Adopt graceful shutdown: fail readiness, stop claims, cancel background work, drain HTTP within 25 seconds, keep PostgreSQL open until admitted work completes, then close; durable leases and idempotency remain crash-safety boundary.
- (review) Reviewer-gate fixes applied: actor-derived owner scoping, lifecycle fences, refresh/disconnect compensation, durable OAuth callback outcomes, concrete CSRF, immutable disclosure versions, module write ownership, onboarding cardinality/readiness, bounded defaults, Render routing spike/fallback, SnapTrade upstream-spec overlay fallback, SDK float32 precision policy, and nullable activity dates.
- (event) spine finalized
